# Similarity Search

| Algorithm | File | Use Case |
|---|---|---|
| Locality-Sensitive Hashing (MinHash) | `lsh_minhash.cpp` | Near-duplicate detection at scale without all-pairs comparison |

Build and run individually with `g++ -std=c++17 -O2 <file>.cpp -o out && ./out`, or `make all` from the repo root.
